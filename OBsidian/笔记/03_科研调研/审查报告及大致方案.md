graph LR
    %% Inputs
    Frgb["F_rgb"]
    Fir["F_ir"]

    %% Branch 1: Illumination Weight
    subgraph "1. 光照权重生成"
        Conv3["Conv 3×3"]
        ReLU["ReLU"]
        Conv1_1["Conv 1×1"]
        Sigmoid["Sigmoid"]
        Will["W_ill"]
        
        Frgb --> Conv3 --> ReLU --> Conv1_1 --> Sigmoid --> Will
    end

    %% Branch 2: Cross-modal Alignment
    subgraph "2. 跨模态对齐"
        Concat1(("© 拼接"))
        ConvOffset["偏移预测卷积"]
        InitialOffset["Initial Offset"]
        Modulate(("⊗ 调制"))
        DeltaP["ΔP (偏移场)"]
        DCN["Deformable Conv"]
        FrgbTilde["F̃_rgb"]

        Frgb --> Concat1
        Fir --> Concat1
        Concat1 --> ConvOffset --> InitialOffset --> Modulate
        Will -.-> Modulate
        Modulate --> DeltaP
        Frgb --> DCN
        DeltaP --> DCN
        DCN --> FrgbTilde
    end

    %% Branch 3: Fusion Phase
    subgraph "3. 融合阶段"
        Mul1(("⊗"))
        Minus1["1 - W_ill"]
        Mul2(("⊗"))
        Concat2(("© 拼接"))
        Conv1_2["Conv 1×1"]
        Ffused["F_fused"]

        FrgbTilde --> Mul1
        Will -.-> Mul1
        Will -.-> Minus1 --> Mul2
        Fir --> Mul2
        Mul1 --> Concat2
        Mul2 --> Concat2
        Concat2 --> Conv1_2 --> Ffused
    end

    %% Branch 4: VIB Regularization (Training Only)
    subgraph "4. VIB 正则分支 (仅训练期)"
        Encoder["高斯变分编码器"]
        MuSigma["μ, σ²"]
        Reparam["重参数化"]
        Z["潜变量 Z"]
        Gent["g_ent"]
        Loss["L_VIB"]

        Ffused -.-> Encoder
        Encoder --> MuSigma --> Reparam --> Z --> Loss
        Gent -.-> Loss
    end